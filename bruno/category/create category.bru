meta {
  name: create category
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{path}}
  body: json
  auth: inherit
}

headers {
  Sec-Fetch-Site: same-origin
}

body:json {
  {
    "name": "pizzas"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
